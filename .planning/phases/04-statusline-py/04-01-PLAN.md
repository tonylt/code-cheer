---
phase: 04-statusline-py
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - statusline.py
  - tests/test_statusline.py
autonomous: true
requirements: [STA-02, CFG-02]

must_haves:
  truths:
    - "render 模式 (无 --update) 不调用 save_state()，不修改 state.json"
    - "--update 首次运行时 session_start 写入 state.json，同天不覆盖，跨天重置"
    - "--debug-events 向 stderr 输出 GIT_CONTEXT / EVENTS_WOULD_FIRE / STATE_SNAPSHOT 三行，stdout 无输出"
    - "render 模式 stdout 仍正常输出两行（角色消息 + token 信息）"
  artifacts:
    - path: "statusline.py"
      provides: "render/update 模式分离, session_start 逻辑, --debug-events stderr 输出"
      contains: "_should_reset_session_start"
    - path: "tests/test_statusline.py"
      provides: "7 个新测试覆盖 STA-02 和 CFG-02"
      min_lines: 200
  key_links:
    - from: "statusline.py main()"
      to: "save_state()"
      via: "仅在 update_only=True 时调用 save_state"
      pattern: "if update_only"
    - from: "statusline.py main()"
      to: "sys.stderr"
      via: "debug_mode 时输出三行 JSON"
      pattern: "file=sys.stderr"
---

<objective>
statusline.py 入口正确分离 render（只读）和 update（写入）模式，集成 session_start 记录和 --debug-events 调试输出。

Purpose: 实现 STA-02（render 模式纯只读）和 CFG-02（--debug-events 可观测性），为 Phase 5 vocab 集成提供稳定的入口层。
Output: 修改后的 statusline.py + 7 个新测试，全套测试通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-statusline-py/04-CONTEXT.md
@.planning/phases/04-statusline-py/04-RESEARCH.md
@.planning/phases/04-statusline-py/04-VALIDATION.md
@.planning/phases/03-event-detection/03-02-SUMMARY.md

<interfaces>
<!-- Key signatures the executor needs. Extracted from codebase. -->

From statusline.py (current save_state signature — to be extended):
```python
def save_state(
    message: str,
    tier: str,
    slot: str,
    last_git_events: list | None = None,
    last_repo: str | None = None,
    commits_today: int | None = None,
) -> None:
```

From statusline.py (current main flow — lines 96, 112, 151-173):
```python
update_only = "--update" in sys.argv
# ...
if update_only and new_last_git_events is not None:
    # save_state(...) — lines 151-166
elif message != state.get("message") or tier != state.get("last_rate_tier"):
    save_state(message, tier, slot=slot)  # <-- line 167-168: RENDER PATH WRITE — must remove per D-01
if update_only:
    return
print(render(character, message, cc_data, stats))  # line 173
```

From statusline.py (constants):
```python
_EMPTY_STATE = {
    "message": "",
    "last_updated": "",
    "last_rate_tier": "normal",
    "last_slot": None,
}
```

From tests/test_statusline.py (existing test pattern):
```python
# monkeypatch pattern for main() tests:
monkeypatch.setattr(sl, 'CONFIG_PATH', str(config_path))
monkeypatch.setattr(sl, 'STATE_PATH', str(state_path))
monkeypatch.setattr(sl, 'STATS_PATH', str(tmp_path / "no.json"))
monkeypatch.setattr(sl, 'BASE_DIR', str(tmp_path))
monkeypatch.setattr(sys, 'argv', ['statusline.py'])
# For git context mock:
monkeypatch.setattr('statusline.load_git_context', lambda cwd: {...})
```

From core/trigger.py (detect_git_events signature — read only, not modified):
```python
def detect_git_events(
    git_context: dict | None,
    state: dict,
    config: dict,
) -> list[str]:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD -- 编写 7 个新测试覆盖 render 只读、session_start、debug-events</name>
  <files>tests/test_statusline.py</files>
  <read_first>
    - tests/test_statusline.py (当前测试文件，了解 monkeypatch 模式和 SAMPLE_CHAR fixture)
    - statusline.py (了解当前 main() 结构和 save_state 签名)
    - .planning/phases/04-statusline-py/04-VALIDATION.md (Wave 0 测试名称和覆盖目标)
  </read_first>
  <behavior>
    - test_render_mode_does_not_write_state: 以 render 模式调用 main()，断言 state.json 内容未被修改（message 字段仍为初始值）
    - test_save_state_session_start: 调用 save_state(..., session_start="2026-04-02T10:00:00")，读取 state.json 验证 session_start 字段存在且值正确
    - test_update_writes_session_start: 以 --update 调用 main()（state.json 无 session_start），断言写入后 state.json 包含 session_start 字段且为今天的 ISO 时间戳
    - test_update_preserves_session_start_same_day: state.json 已有今天的 session_start="2026-04-02T08:00:00"，以 --update 调用 main()，断言 session_start 保持 "2026-04-02T08:00:00" 不被覆盖
    - test_update_resets_session_start_cross_day: state.json 有昨天的 session_start="2026-04-01T08:00:00"，以 --update 调用 main()，断言 session_start 被重置为今天的值
    - test_debug_events_stderr_output: 以 --debug-events 调用 main()，断言 capsys.readouterr().err 包含 "GIT_CONTEXT:" 和 "EVENTS_WOULD_FIRE:" 和 "STATE_SNAPSHOT:"
    - test_debug_events_no_stdout: 以 --debug-events 调用 main()，断言 capsys.readouterr().out == ""
  </behavior>
  <action>
    在 tests/test_statusline.py 末尾新增 7 个测试函数。遵循已有模式：使用 tmp_path、monkeypatch、capsys、SAMPLE_CHAR、write_json helper。

    **测试 1: test_render_mode_does_not_write_state (STA-02 per D-01)**
    - 写入初始 state.json: {"message": "old_msg", "last_updated": "old", "last_rate_tier": "normal", "last_slot": null}
    - sys.argv = ['statusline.py'] (无 --update)
    - monkeypatch stdin 为 '{}'
    - 调用 sl.main()
    - 重新读取 state.json，断言 message 仍为 "old_msg"（render 路径不写 state）

    **测试 2: test_save_state_session_start (D-03)**
    - 调用 sl.save_state("msg", "normal", "morning", session_start="2026-04-02T10:00:00")
    - 读取 state.json，断言 s["session_start"] == "2026-04-02T10:00:00"

    **测试 3: test_update_writes_session_start (D-02)**
    - state.json 无 session_start 字段
    - sys.argv = ['statusline.py', '--update']
    - monkeypatch load_git_context 返回 {"commits_today": 0, "diff_lines": 0, "repo_path": "/tmp/repo", "first_commit_time": None}
    - 调用 sl.main()
    - 读取 state.json，断言 "session_start" in saved 且值包含今天日期字符串 datetime.now().strftime("%Y-%m-%d")

    **测试 4: test_update_preserves_session_start_same_day (D-02)**
    - state.json 含 session_start = datetime.now().replace(hour=8, minute=0).isoformat()（今天 08:00）
    - sys.argv = ['statusline.py', '--update']
    - monkeypatch load_git_context 返回空 context
    - 调用 sl.main()
    - 读取 state.json，断言 session_start 仍为原始 "..T08:00:..." 值

    **测试 5: test_update_resets_session_start_cross_day (D-02)**
    - state.json 含 session_start = (datetime.now() - timedelta(days=1)).isoformat()（昨天）
    - sys.argv = ['statusline.py', '--update']
    - monkeypatch load_git_context 返回空 context
    - 调用 sl.main()
    - 读取 state.json，断言 session_start 包含今天日期

    **测试 6: test_debug_events_stderr_output (CFG-02 per D-04)**
    - sys.argv = ['statusline.py', '--debug-events']
    - monkeypatch load_git_context 返回 {"commits_today": 3, "diff_lines": 50, "repo_path": "/tmp/repo", "first_commit_time": None}
    - 调用 sl.main()
    - captured = capsys.readouterr()
    - 断言 "GIT_CONTEXT:" in captured.err
    - 断言 "EVENTS_WOULD_FIRE:" in captured.err
    - 断言 "STATE_SNAPSHOT:" in captured.err

    **测试 7: test_debug_events_no_stdout (CFG-02 per D-04)**
    - 与测试 6 相同 setup
    - 断言 captured.out == ""

    所有测试此时应 FAIL（RED 阶段），因为 statusline.py 尚未实现这些功能。运行确认至少 test_render_mode_does_not_write_state 失败（因为当前 render 路径仍会写 state）。
  </action>
  <verify>
    <automated>cd /Users/tony/workspace/ai/code-pal && python3 -m pytest tests/test_statusline.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_statusline.py 包含函数 def test_render_mode_does_not_write_state
    - tests/test_statusline.py 包含函数 def test_save_state_session_start
    - tests/test_statusline.py 包含函数 def test_update_writes_session_start
    - tests/test_statusline.py 包含函数 def test_update_preserves_session_start_same_day
    - tests/test_statusline.py 包含函数 def test_update_resets_session_start_cross_day
    - tests/test_statusline.py 包含函数 def test_debug_events_stderr_output
    - tests/test_statusline.py 包含函数 def test_debug_events_no_stdout
    - test_render_mode_does_not_write_state 断言 state.json message 未被 render 路径修改
    - test_debug_events_stderr_output 断言 captured.err 包含 "GIT_CONTEXT:"
    - 已有测试（test_main_prints_two_lines 等）仍然通过
  </acceptance_criteria>
  <done>7 个新测试函数存在于 tests/test_statusline.py，已有测试不受影响，新测试中至少 test_render_mode_does_not_write_state 处于 RED 状态（预期失败）</done>
</task>

<task type="auto">
  <name>Task 2: 实现 render 只读 + session_start + --debug-events（GREEN 阶段）</name>
  <files>statusline.py</files>
  <read_first>
    - statusline.py (当前完整代码，特别是 line 68-92 save_state 和 line 95-177 main)
    - tests/test_statusline.py (Task 1 新增的测试，了解预期行为)
    - .planning/phases/04-statusline-py/04-CONTEXT.md (D-01 到 D-04 锁定决策)
    - .planning/phases/04-statusline-py/04-RESEARCH.md (Pattern 1-5 实现模式和 Pitfall 1-5)
  </read_first>
  <action>
    修改 statusline.py，实现 3 个功能点。按以下顺序修改：

    **1. save_state() 新增 session_start 参数 (per D-03)**

    在 save_state() 签名中新增 `session_start: str | None = None` 参数（位于 commits_today 之后）。在函数体中，在 `if commits_today is not None:` 块之后添加：
    ```python
    if session_start is not None:
        state["session_start"] = session_start
    ```
    保持与已有 optional 参数完全一致的 `is not None` 模式。

    **2. 新增 _should_reset_session_start() 辅助函数 (per D-02)**

    在 save_state() 函数之后（main() 之前）添加：
    ```python
    def _should_reset_session_start(existing: str | None) -> bool:
        if not existing:
            return True
        try:
            dt = datetime.fromisoformat(existing)
            return dt.date() != datetime.now().date()
        except (ValueError, TypeError):
            return True
    ```

    **3. main() 修改 — 三个变更点**

    3a. 参数检测（line 96）替换为：
    ```python
    update_only = "--update" in sys.argv or "--debug-events" in sys.argv
    debug_mode = "--debug-events" in sys.argv
    ```

    3b. session_start 逻辑 — 在 `if update_only:` 分支内（git_context 加载之后、resolve_message 之前），添加：
    ```python
    session_start_val = state.get("session_start")
    if _should_reset_session_start(session_start_val):
        session_start_val = datetime.now().isoformat()
    ```
    然后在两处 save_state() 调用中都传入 `session_start=session_start_val`。

    注意：session_start_val 变量需要在 update_only 块之外也能被 debug_mode 块访问。可以在 main() 开头初始化为 `session_start_val = None`，在 update_only 块内赋值。

    3c. 移除 render 路径的 save_state 调用 (per D-01) — 删除 line 167-168：
    ```python
    # 删除这两行:
    elif message != state.get("message") or tier != state.get("last_rate_tier"):
        save_state(message, tier, slot=slot)
    ```
    修改后的逻辑：`if update_only and new_last_git_events is not None:` 块保留（两处 save_state 调用均传入 session_start=session_start_val），`elif` 分支整体删除。render 路径直接走到 `if update_only: return` 再到 `print(render(...))`.

    但需要处理 update_only 但 git_context 为 None 的情况（比如非 git 目录）。此时 new_last_git_events 为 None，现有 `if update_only and new_last_git_events is not None:` 不会执行。需要在此条件之后添加：
    ```python
    elif update_only:
        # update mode but no git context (non-git dir) — still save message + session_start
        save_state(message, tier, slot=slot, session_start=session_start_val)
    ```

    3d. --debug-events stderr 输出 (per D-04) — 在 `if update_only: return` 之前添加：
    ```python
    if debug_mode:
        session_start_dt = datetime.fromisoformat(session_start_val)
        session_minutes = int((datetime.now() - session_start_dt).total_seconds() / 60)
        git_ctx_display = {
            "commits_today": git_context.get("commits_today", 0) if git_context else 0,
            "diff_lines": git_context.get("diff_lines", 0) if git_context else 0,
            "session_minutes": session_minutes,
        }
        events_display = {}
        for e in (triggered_events or []):
            events_display[e] = _event_reason(e, git_context or {}, config)
        state_snapshot = {
            "last_git_events": new_last_git_events or [],
            "commits_today": new_commits_today or 0,
            "session_start": session_start_val,
            "last_repo": new_last_repo,
        }
        ts = datetime.now().isoformat(timespec="seconds")
        print(f"[{ts}] GIT_CONTEXT: {json.dumps(git_ctx_display)}", file=sys.stderr)
        print(f"EVENTS_WOULD_FIRE: {json.dumps(events_display)}", file=sys.stderr)
        print(f"STATE_SNAPSHOT: {json.dumps(state_snapshot, ensure_ascii=False)}", file=sys.stderr)
    ```

    3e. 新增 _event_reason() 辅助函数（在 _should_reset_session_start 之后）：
    ```python
    def _event_reason(event: str, git_context: dict, config: dict) -> str:
        thresholds = config.get("event_thresholds", {})
        if event == "first_commit_today":
            return "first commit of the day"
        elif event.startswith("milestone_"):
            count = event.split("_")[1]
            return f"commits_today {git_context.get('commits_today', 0)} reached milestone {count}"
        elif event == "late_night_commit":
            hour = thresholds.get("late_night_hour_start", 22)
            return f"current hour >= threshold {hour}"
        elif event == "big_diff":
            threshold = thresholds.get("big_diff", 200)
            return f"diff_lines {git_context.get('diff_lines', 0)} >= threshold {threshold}"
        elif event == "big_session":
            threshold = thresholds.get("big_session_minutes", 120)
            return f"session_minutes >= threshold {threshold}"
        elif event == "long_day":
            threshold = thresholds.get("long_day_commits", 15)
            return f"commits_today {git_context.get('commits_today', 0)} >= threshold {threshold}"
        return event
    ```

    修改完成后，运行全量测试确认所有 7 个新测试 + 所有已有测试通过。
  </action>
  <verify>
    <automated>cd /Users/tony/workspace/ai/code-pal && python3 -m pytest tests/ -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - statusline.py 包含 def _should_reset_session_start(existing: str | None) -> bool
    - statusline.py 包含 def _event_reason(event: str, git_context: dict, config: dict) -> str
    - statusline.py save_state 签名包含 session_start: str | None = None
    - statusline.py 包含 debug_mode = "--debug-events" in sys.argv
    - statusline.py 包含 print(f"[{ts}] GIT_CONTEXT: 且使用 file=sys.stderr
    - statusline.py 不包含 elif message != state.get("message") or tier != state.get("last_rate_tier"): 后跟 save_state(message, tier, slot=slot) 的旧 render 路径写入
    - python3 -m pytest tests/ -q 退出码为 0（全部通过，含 7 个新测试）
    - python3 -m pytest tests/test_statusline.py::test_render_mode_does_not_write_state -x 退出码为 0
    - python3 -m pytest tests/test_statusline.py::test_debug_events_stderr_output -x 退出码为 0
    - python3 -m pytest tests/test_statusline.py::test_debug_events_no_stdout -x 退出码为 0
  </acceptance_criteria>
  <done>statusline.py 实现 render 纯只读、session_start 记录/重置、--debug-events stderr 输出；全量测试通过（110+ 已有 + 7 新增）</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/ -q` — 全量测试通过（117+ tests）
- `python3 -m pytest tests/test_statusline.py::test_render_mode_does_not_write_state -x` — render 不写 state
- `python3 -m pytest tests/test_statusline.py::test_debug_events_stderr_output -x` — stderr 三行输出
- `python3 -m pytest tests/test_statusline.py::test_debug_events_no_stdout -x` — stdout 为空
- `python3 -m pytest tests/test_statusline.py::test_update_writes_session_start -x` — 首次写入
- `python3 -m pytest tests/test_statusline.py::test_update_preserves_session_start_same_day -x` — 同天保持
- `python3 -m pytest tests/test_statusline.py::test_update_resets_session_start_cross_day -x` — 跨天重置
- `python3 -m pytest tests/test_statusline.py::test_save_state_session_start -x` — save_state 新参数
</verification>

<success_criteria>
1. render 模式（无 --update）不调用 save_state()，state.json 内容在 render 后不变
2. --update 首次运行在 state.json 写入 session_start（今天 ISO 时间戳），同天不覆盖，跨天重置
3. --debug-events 向 stderr 输出 GIT_CONTEXT / EVENTS_WOULD_FIRE / STATE_SNAPSHOT 三行 JSON，stdout 无输出
4. 全量 pytest 通过（117+ tests），无回归
</success_criteria>

<output>
After completion, create `.planning/phases/04-statusline-py/04-01-SUMMARY.md`
</output>
