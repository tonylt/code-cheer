---
phase: 05-vocab
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - vocab/nova.json
  - vocab/luna.json
  - vocab/mochi.json
  - vocab/iris.json
autonomous: true
requirements:
  - TST-02
user_setup: []

must_haves:
  truths:
    - "Each of the 4 vocab files (nova/luna/mochi/iris) contains a top-level git_events key"
    - "Each git_events section has all 8 event keys with exactly 3 messages each"
    - "Messages reference the specific event context (not generic encouragement)"
    - "Each character's messages match their established personality style"
    - "Existing vocab structure (meta, triggers) is unchanged"
  artifacts:
    - path: "vocab/nova.json"
      provides: "Nova git_events messages (8 keys x 3 = 24 messages)"
      contains: "git_events"
    - path: "vocab/luna.json"
      provides: "Luna git_events messages (8 keys x 3 = 24 messages)"
      contains: "git_events"
    - path: "vocab/mochi.json"
      provides: "Mochi git_events messages (8 keys x 3 = 24 messages)"
      contains: "git_events"
    - path: "vocab/iris.json"
      provides: "Iris git_events messages (8 keys x 3 = 24 messages)"
      contains: "git_events"
  key_links:
    - from: "vocab/*.json git_events keys"
      to: "core/trigger.py resolve_message()"
      via: "character.get('git_events', {}).get(event_key)"
      pattern: "first_commit_today|milestone_5|milestone_10|milestone_20|late_night_commit|big_diff|big_session|long_day"
---

<objective>
Add `git_events` top-level section to all 4 character vocab JSON files with 96 total messages (8 event keys x 3 messages x 4 characters).

Purpose: Provide the actual content that makes git-event-driven character reactions work. Without these messages, `get_git_event_message()` returns None and falls back to generic post_tool messages.

Output: Updated nova.json, luna.json, mochi.json, iris.json — each with a new `git_events` section containing 24 event-specific messages in character voice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-vocab/05-CONTEXT.md

@vocab/nova.json
@vocab/luna.json
@vocab/mochi.json
@vocab/iris.json

<interfaces>
<!-- The 8 event keys that trigger.py's detect_git_events() produces -->
Event keys (from core/trigger.py detect_git_events()):
- first_commit_today: User's first git commit of the day
- milestone_5: User reached 5 commits today
- milestone_10: User reached 10 commits today
- milestone_20: User reached 20 commits today
- late_night_commit: User committed after 22:00 (configurable)
- big_diff: HEAD diff >= 200 lines (configurable)
- big_session: Session duration >= 120 minutes (configurable)
- long_day: Commits today >= 15 (configurable)

Character styles (from vocab meta.style):
- Nova: 元气满满，感叹号连发，运动系加油风 (energetic, exclamation marks, sporty cheerleader)
- Luna: 温柔治愈，陪伴系，轻声细语 (gentle, healing, companion, soft-spoken)
- Mochi: 软萌奶凶，傲娇猫系，第三人称自称 (soft-cute but fierce, tsundere cat, third-person self-reference)
- Iris: 女王御姐，冷静挑衅，激将法 (queenly, cool provocative, reverse psychology)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add git_events section to nova.json and luna.json</name>
  <files>vocab/nova.json, vocab/luna.json</files>
  <read_first>
    - vocab/nova.json (full file — see existing structure: meta + triggers at top level, see random messages for style reference)
    - vocab/luna.json (full file — see existing structure and Luna's gentle style)
  </read_first>
  <action>
Add a `"git_events"` key at the **top level** of each JSON file (after `"triggers"`, per D-01), containing all 8 event keys with exactly 3 messages each.

**JSON structure to add** (inserted after the closing `}` of `"triggers"` and before the final `}`):

```json
"git_events": {
    "first_commit_today": ["msg1", "msg2", "msg3"],
    "milestone_5": ["msg1", "msg2", "msg3"],
    "milestone_10": ["msg1", "msg2", "msg3"],
    "milestone_20": ["msg1", "msg2", "msg3"],
    "late_night_commit": ["msg1", "msg2", "msg3"],
    "big_diff": ["msg1", "msg2", "msg3"],
    "big_session": ["msg1", "msg2", "msg3"],
    "long_day": ["msg1", "msg2", "msg3"]
}
```

**Message content guidelines (per D-05):**
- Length: similar to existing `random` messages (roughly 15-25 Chinese characters)
- Content: MUST reference the specific event — "first commit today", "5th commit", "10th commit", "20th commit", "late night coding", "big code change", "long session", "long day"
- Style: match each character's personality from `meta.style`

**Nova style:** Energetic, exclamation marks, sporty cheerleader energy. Uses "!!", "GO!", "冲!" frequently.
Example event messages:
- first_commit_today: Celebrate the day's first commit with explosive energy
- milestone_5: "5 commits" achievement unlocked energy
- late_night_commit: Late night but still fired up
- big_diff: Big code changes = big power

**Luna style:** Gentle, soft-spoken, healing companion. Uses "呢", "哦", "～" for softness.
Example event messages:
- first_commit_today: Warm, gentle acknowledgment of starting the day
- milestone_5: Quietly proud, gentle encouragement
- late_night_commit: Caring concern mixed with admiration
- big_diff: Gentle amazement at the volume of work

Do NOT modify existing `meta` or `triggers` sections. Only add the new `git_events` key. Ensure valid JSON after editing.
  </action>
  <verify>
    <automated>cd /Users/tony/workspace/ai/code-pal && python3 -c "
import json
for name in ['nova', 'luna']:
    with open(f'vocab/{name}.json') as f:
        d = json.load(f)
    ge = d.get('git_events', {})
    keys = ['first_commit_today','milestone_5','milestone_10','milestone_20','late_night_commit','big_diff','big_session','long_day']
    assert set(ge.keys()) == set(keys), f'{name}: missing keys {set(keys) - set(ge.keys())}'
    for k in keys:
        assert len(ge[k]) == 3, f'{name}.{k}: expected 3 messages, got {len(ge[k])}'
        for msg in ge[k]:
            assert isinstance(msg, str) and len(msg) > 0, f'{name}.{k}: empty message'
    assert 'meta' in d and 'triggers' in d, f'{name}: meta/triggers missing'
    print(f'{name}.json: OK (8 keys, 24 messages)')
print('ALL PASS')
"</automated>
  </verify>
  <acceptance_criteria>
    - vocab/nova.json contains top-level "git_events" key (not inside "triggers")
    - vocab/nova.json git_events has exactly 8 keys: first_commit_today, milestone_5, milestone_10, milestone_20, late_night_commit, big_diff, big_session, long_day
    - vocab/nova.json each event key has exactly 3 string messages
    - vocab/luna.json contains top-level "git_events" key
    - vocab/luna.json git_events has exactly 8 keys with 3 messages each
    - Both files remain valid JSON (python3 json.load succeeds)
    - Both files retain existing "meta" and "triggers" sections unchanged
  </acceptance_criteria>
  <done>nova.json and luna.json each have 24 event-specific messages in their character voice, valid JSON, existing content unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Add git_events section to mochi.json and iris.json</name>
  <files>vocab/mochi.json, vocab/iris.json</files>
  <read_first>
    - vocab/mochi.json (full file — see existing structure and Mochi's tsundere cat style, third-person self-reference "Mochi")
    - vocab/iris.json (full file — see existing structure and Iris's queenly provocative style)
  </read_first>
  <action>
Add a `"git_events"` key at the **top level** of each JSON file (after `"triggers"`, per D-01), containing all 8 event keys with exactly 3 messages each. Same structure as Task 1.

**Mochi style:** Soft-cute tsundere cat, third-person self-reference ("Mochi"), alternates between sweet and fierce. Uses "喵", "哼", references self as "Mochi" or "本喵".
Example event messages:
- first_commit_today: Tsundere reaction to first commit ("不是 Mochi 在等你开始写代码喵...")
- milestone_5: Pretends not to care but is actually impressed
- late_night_commit: Scolds for staying up but secretly happy
- big_diff: Impressed but tries to hide it

**Iris style:** Queenly, cool provocative, reverse psychology motivation. Uses formal/commanding tone, challenges the user. Uses "罢了", "而已", "不过如此".
Example event messages:
- first_commit_today: Cool acknowledgment, implies "about time"
- milestone_5: Backhanded compliment, "not bad for a start"
- late_night_commit: Questions if the user can handle it
- big_diff: Acknowledges the scale but raises the bar higher

Do NOT modify existing `meta` or `triggers` sections. Only add the new `git_events` key. Ensure valid JSON after editing.
  </action>
  <verify>
    <automated>cd /Users/tony/workspace/ai/code-pal && python3 -c "
import json
for name in ['mochi', 'iris']:
    with open(f'vocab/{name}.json') as f:
        d = json.load(f)
    ge = d.get('git_events', {})
    keys = ['first_commit_today','milestone_5','milestone_10','milestone_20','late_night_commit','big_diff','big_session','long_day']
    assert set(ge.keys()) == set(keys), f'{name}: missing keys {set(keys) - set(ge.keys())}'
    for k in keys:
        assert len(ge[k]) == 3, f'{name}.{k}: expected 3 messages, got {len(ge[k])}'
        for msg in ge[k]:
            assert isinstance(msg, str) and len(msg) > 0, f'{name}.{k}: empty message'
    assert 'meta' in d and 'triggers' in d, f'{name}: meta/triggers missing'
    print(f'{name}.json: OK (8 keys, 24 messages)')
# Full suite regression
print('Validating all 4 vocab files...')
for name in ['nova', 'luna', 'mochi', 'iris']:
    with open(f'vocab/{name}.json') as f:
        json.load(f)
    print(f'  {name}.json: valid JSON')
print('ALL PASS')
"</automated>
  </verify>
  <acceptance_criteria>
    - vocab/mochi.json contains top-level "git_events" key with 8 event keys, 3 messages each
    - vocab/iris.json contains top-level "git_events" key with 8 event keys, 3 messages each
    - Both files remain valid JSON
    - Both files retain existing "meta" and "triggers" sections unchanged
    - All 4 vocab files (nova/luna/mochi/iris) load without error
  </acceptance_criteria>
  <done>All 4 vocab files have git_events sections with 96 total messages (8 events x 3 messages x 4 characters), all valid JSON, all existing content preserved</done>
</task>

</tasks>

<verification>
```bash
# All 4 vocab files valid JSON with git_events
python3 -c "
import json
for name in ['nova', 'luna', 'mochi', 'iris']:
    with open(f'vocab/{name}.json') as f:
        d = json.load(f)
    ge = d['git_events']
    assert len(ge) == 8
    for k, v in ge.items():
        assert len(v) == 3
    print(f'{name}: 8 events, 24 messages')
"

# Full test suite still passes (vocab changes should not break tests)
python3 -m pytest tests/ -q
```
</verification>

<success_criteria>
1. All 4 vocab JSON files contain top-level `git_events` key (per D-01)
2. Each file has all 8 event keys: first_commit_today, milestone_5, milestone_10, milestone_20, late_night_commit, big_diff, big_session, long_day
3. Each event key has exactly 3 messages (per D-04: 8 x 4 x 3 = 96 total)
4. Messages reference specific events, not generic encouragement (per D-05)
5. Message style matches each character's personality
6. Existing meta and triggers sections are unchanged
7. All files are valid JSON
</success_criteria>

<output>
After completion, create `.planning/phases/05-vocab/05-02-SUMMARY.md`
</output>
