---
phase: 08-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - src/statusline.ts
  - src/core/display.ts
  - src/core/character.ts
  - src/core/trigger.ts
  - src/core/gitContext.ts
  - .gitignore
autonomous: true
requirements:
  - SETUP-01
must_haves:
  truths:
    - "npm run build produces dist/statusline.js"
    - "npm run typecheck passes with no errors"
    - "node dist/statusline.js exits 0 in <100ms"
    - "All src/ stub files are valid TypeScript modules"
  artifacts:
    - path: "package.json"
      provides: "npm scripts (build, typecheck) and devDependencies"
      contains: "esbuild src/statusline.ts --bundle"
    - path: "tsconfig.json"
      provides: "TypeScript strict configuration"
      contains: '"strict": true'
    - path: "src/statusline.ts"
      provides: "Entry point stub"
      contains: "export {}"
    - path: "src/core/display.ts"
      provides: "Display module stub"
      contains: "export {}"
    - path: "src/core/character.ts"
      provides: "Character module stub"
      contains: "export {}"
    - path: "src/core/trigger.ts"
      provides: "Trigger module stub"
      contains: "export {}"
    - path: "src/core/gitContext.ts"
      provides: "Git context module stub"
      contains: "export {}"
    - path: "dist/statusline.js"
      provides: "esbuild bundle output (not committed)"
  key_links:
    - from: "package.json"
      to: "dist/statusline.js"
      via: "npm run build (esbuild CLI)"
      pattern: "esbuild src/statusline\\.ts.*--outfile=dist/statusline\\.js"
    - from: "tsconfig.json"
      to: "src/**/*.ts"
      via: "include glob"
      pattern: '"include".*src/\\*\\*/\\*'
---

<objective>
Create the TypeScript project scaffolding: package.json with esbuild build script, tsconfig.json with strict mode, src/ directory with stub module files, and .gitignore updates. Verify the build pipeline produces a working dist/statusline.js with <100ms cold start.

Purpose: Establish the TypeScript toolchain foundation that all subsequent phases (09-13) build upon. Per D-01, the build output must be a single-file CJS bundle via esbuild.
Output: Working `npm run build` and `npm run typecheck` commands, stub source files, package-lock.json committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ci/08-CONTEXT.md
@.planning/phases/08-ci/08-RESEARCH.md
@.github/workflows/ci.yml
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package.json, tsconfig.json, src/ stubs, and update .gitignore</name>
  <files>package.json, tsconfig.json, src/statusline.ts, src/core/display.ts, src/core/character.ts, src/core/trigger.ts, src/core/gitContext.ts, .gitignore</files>
  <read_first>.gitignore, .planning/phases/08-ci/08-RESEARCH.md</read_first>
  <action>
Create the following files with exact content:

**package.json** (per D-01, D-02, D-04):
```json
{
  "name": "code-pal",
  "version": "3.0.0",
  "description": "Claude Code statusline companion",
  "private": true,
  "scripts": {
    "build": "esbuild src/statusline.ts --bundle --platform=node --format=cjs --outfile=dist/statusline.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.0"
  }
}
```

**tsconfig.json** (per D-09, D-10):
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "CommonJS",
    "lib": ["ES2020"],
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "declaration": false,
    "sourceMap": false
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

**5 src/ stub files** (per D-12, D-13) — each file contains exactly one line:
```typescript
export {};
```

Files to create:
- `src/statusline.ts`
- `src/core/display.ts`
- `src/core/character.ts`
- `src/core/trigger.ts`
- `src/core/gitContext.ts`

**Append to .gitignore** — add these lines at the end of the existing file:
```
# Node.js
node_modules/
npm-debug.log*
```

Note: `dist/` is already in .gitignore (line 14 of existing file, under "Distribution / packaging"). Verify it covers the root `dist/` directory. If the existing `dist/` entry is sufficient, do not add a duplicate.

After creating all files, run:
```bash
npm install
```
This generates `package-lock.json` which must be committed to git (required for `npm ci` in CI per Pitfall 2).
  </action>
  <verify>
    <automated>test -f package.json && test -f tsconfig.json && test -f src/statusline.ts && test -f src/core/display.ts && test -f src/core/character.ts && test -f src/core/trigger.ts && test -f src/core/gitContext.ts && test -f package-lock.json && grep '"build"' package.json && grep '"strict": true' tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
- package.json contains `"build": "esbuild src/statusline.ts --bundle --platform=node --format=cjs --outfile=dist/statusline.js"`
- package.json contains `"typecheck": "tsc --noEmit"`
- package.json contains `"private": true`
- package.json contains `"typescript": "^6.0.0"` in devDependencies
- package.json contains `"esbuild": "^0.28.0"` in devDependencies
- package.json contains `"@types/node": "^25.0.0"` in devDependencies
- tsconfig.json contains `"strict": true`
- tsconfig.json contains `"target": "ES2020"`
- tsconfig.json contains `"module": "CommonJS"`
- tsconfig.json contains `"rootDir": "./src"`
- All 5 src/ files contain `export {};`
- package-lock.json exists (generated by npm install)
- .gitignore contains `node_modules/`
- node_modules/ directory exists (npm install succeeded)
  </acceptance_criteria>
  <done>All config files created, src/ stubs in place, npm install succeeded, package-lock.json generated</done>
</task>

<task type="auto">
  <name>Task 2: Verify build pipeline — npm run build, typecheck, and cold start</name>
  <files>dist/statusline.js</files>
  <read_first>package.json, tsconfig.json, src/statusline.ts</read_first>
  <action>
Run the three verification commands in sequence:

1. **Build** (per D-01): `npm run build`
   - Expected: esbuild produces `dist/statusline.js` with exit code 0
   - If esbuild fails, the error message should be visible in stdout/stderr (per D-04)

2. **Type check** (per D-02): `npm run typecheck`
   - Expected: `tsc --noEmit` passes with exit code 0 and no output (no type errors)

3. **Cold start validation** (per SETUP-01):
   ```bash
   START=$(date +%s%3N)
   node dist/statusline.js
   END=$(date +%s%3N)
   MS=$((END - START))
   echo "Cold start: ${MS}ms"
   if [ "$MS" -gt 100 ]; then
     echo "FAIL: cold start ${MS}ms exceeds 100ms threshold"
     exit 1
   fi
   echo "PASS: cold start ${MS}ms"
   ```
   - Expected: <100ms (empty bundle ~12-18ms typical)

4. **Python test regression check**: `python3 -m pytest tests/`
   - Expected: All existing Python tests still pass (no accidental breakage)

If any step fails, diagnose and fix the root cause before proceeding.
  </action>
  <verify>
    <automated>npm run build && test -f dist/statusline.js && npm run typecheck && node dist/statusline.js && python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
- `npm run build` exits 0
- `dist/statusline.js` file exists after build
- `npm run typecheck` exits 0 with no error output
- `node dist/statusline.js` exits 0
- Cold start time <100ms (measured via date +%s%3N)
- `python3 -m pytest tests/` exits 0 (no regression)
  </acceptance_criteria>
  <done>Build pipeline fully operational: esbuild produces dist/statusline.js, tsc passes, cold start <100ms, Python tests unbroken</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds and produces `dist/statusline.js`
- `npm run typecheck` passes with zero errors
- `node dist/statusline.js` runs in <100ms
- All existing Python tests pass (`python3 -m pytest tests/`)
- `package-lock.json` exists and is committed
</verification>

<success_criteria>
1. `dist/statusline.js` exists after `npm run build` (SETUP-01)
2. `tsc --noEmit` exits 0 (SETUP-01)
3. Cold start <100ms (SETUP-01)
4. Python tests unbroken (regression safety)
</success_criteria>

<output>
After completion, create `.planning/phases/08-ci/08-01-SUMMARY.md`
</output>
