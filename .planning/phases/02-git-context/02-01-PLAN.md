---
phase: 02-git-context
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - core/git_context.py
  - tests/test_git_context.py
autonomous: true
requirements:
  - STA-03
must_haves:
  truths:
    - "load_git_context('/some/repo') returns a dict with commits_today, diff_lines, first_commit_time, repo_path when git is available"
    - "load_git_context('/not-a-repo') returns all-zero/None defaults when not in a git repo"
    - "If any single subprocess times out or fails, the corresponding field falls back to its default while other fields remain correct"
    - "If git is not installed (FileNotFoundError), all fields fall back to safe defaults and no exception propagates"
    - "diff_lines equals insertions + deletions (not just insertions)"
    - "first_commit_time is the earliest commit today (last line of git log output), not the latest"
    - "4 subprocesses run in parallel via ThreadPoolExecutor"
    - "When load_git_context() returns fallback values (all-zero/None defaults), statusline.py can still render without errors — the statusline displays normally"
  artifacts:
    - path: "core/git_context.py"
      provides: "load_git_context(cwd) function + _run_git_cmd helper + 4 parse functions"
      exports: ["load_git_context"]
      contains: "SUBPROCESS_TIMEOUT = 5"
    - path: "tests/test_git_context.py"
      provides: "Unit tests for all git context paths"
      min_lines: 80
  key_links:
    - from: "core/git_context.py"
      to: "subprocess.run"
      via: "_run_git_cmd calls subprocess.run with timeout=5, check=False"
      pattern: "subprocess\\.run\\("
    - from: "core/git_context.py"
      to: "concurrent.futures.ThreadPoolExecutor"
      via: "load_git_context uses ThreadPoolExecutor(max_workers=4)"
      pattern: "ThreadPoolExecutor"
    - from: "tests/test_git_context.py"
      to: "core/git_context.py"
      via: "from core.git_context import load_git_context"
      pattern: "from core\\.git_context import"
---

<objective>
Create `core/git_context.py` with TDD: write failing tests first, then implement `load_git_context(cwd)` that reads git state via 4 parallel subprocesses with silent fallback on any failure.

Purpose: STA-03 requires that any git subprocess failure (timeout, no repo, git not installed) silently degrades to safe defaults without breaking the statusline. This module is the foundation for Phase 3 event detection.

Output: Working `core/git_context.py` + comprehensive `tests/test_git_context.py`, all tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-git-context/02-CONTEXT.md
@.planning/phases/02-git-context/02-RESEARCH.md
@.planning/phases/02-git-context/02-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@core/trigger.py
@tests/test_trigger.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for load_git_context (RED)</name>
  <files>tests/test_git_context.py</files>
  <read_first>
    - tests/test_trigger.py (reference test pattern: sys.path.insert, unittest.mock.patch usage, assertion style)
    - .planning/phases/02-git-context/02-CONTEXT.md (locked decisions D-01 through D-06)
    - .planning/phases/02-git-context/02-RESEARCH.md (test mock patterns in Code Examples section)
  </read_first>
  <behavior>
    - test_normal_output: Mock subprocess.run to return valid output for all 4 commands. Assert commits_today == 2 (2 oneline log entries), diff_lines == 57 (45 insertions + 12 deletions), first_commit_time == '2026-04-01 09:00:00 +0800' (last line = earliest), repo_path == '/Users/user/project'
    - test_diff_lines_sum: Mock only diff --stat output with "3 files changed, 100 insertions(+), 50 deletions(-)" and assert diff_lines == 150 (per D-02: insertions + deletions)
    - test_clean_working_dir: Mock diff --stat to return empty string. Assert diff_lines == 0
    - test_diff_insertions_only: Mock diff --stat output "1 file changed, 20 insertions(+)" (no deletions line). Assert diff_lines == 20
    - test_diff_deletions_only: Mock diff --stat output "1 file changed, 10 deletions(-)" (no insertions). Assert diff_lines == 10
    - test_first_commit_time_earliest: Mock git log --format=%ai to return 3 lines (newest first per git default). Assert first_commit_time is the LAST line (earliest commit today)
    - test_first_commit_time_single: Mock single commit line. Assert that line is returned as first_commit_time
    - test_all_fail_returns_safe_defaults: Mock subprocess.run to raise Exception('git not found'). Assert result == {'commits_today': 0, 'diff_lines': 0, 'first_commit_time': None, 'repo_path': None}
    - test_git_not_installed: Mock subprocess.run to raise FileNotFoundError. Assert same safe defaults, no exception propagates
    - test_timeout_fallback: Mock subprocess.run to raise subprocess.TimeoutExpired for one command but succeed for others. Assert failed field is default, other fields are correct
    - test_partial_failure: Mock one subprocess to return returncode=128 (fatal: not a git repo), others succeed. Assert failed field is default, others parsed correctly
    - test_non_git_dir: Mock all 4 subprocess.run calls to return returncode=128. Assert all-zero/None defaults
    - test_no_commits_today: Mock git log --oneline to return empty string. Assert commits_today == 0, first_commit_time == None
  </behavior>
  <action>
    Create `tests/test_git_context.py` following project test conventions:

    1. File header (matching test_trigger.py pattern):
       ```python
       import os, sys, subprocess
       from unittest.mock import patch, MagicMock
       sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
       from core.git_context import load_git_context
       ```

    2. Helper function to create mock subprocess.run responses:
       ```python
       def _make_result(stdout='', returncode=0):
           m = MagicMock()
           m.stdout = stdout
           m.returncode = returncode
           return m
       ```

    3. For the `fake_run` dispatcher used by test_normal_output, route based on args content:
       - `'log' in args and '--oneline' in args` -> 2 commit lines
       - `'diff' in args and '--stat' in args` -> " 2 files changed, 45 insertions(+), 12 deletions(-)\n"
       - `'log' in args and '--format=%ai' in args` -> two ISO timestamps, newest first, oldest last
       - `'rev-parse' in args` -> "/Users/user/project\n"

    4. All patches target `subprocess.run` (matching RESEARCH.md pattern: `patch('subprocess.run', side_effect=...)`).

    5. Tests should be runnable but FAIL because `core/git_context.py` does not exist yet. The import will cause ImportError. This is the RED phase.

    After writing, run: `python3 -m pytest tests/test_git_context.py -v 2>&1 | head -30` to confirm tests are collected but fail (ImportError or assertion failures).
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_git_context.py --collect-only 2>&1 | grep -c "test_" || echo "0"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_git_context.py exists and contains at least 13 test functions
    - File starts with `import os, sys` and `sys.path.insert(0,` matching project convention
    - File contains `from core.git_context import load_git_context`
    - File contains `patch('subprocess.run'` (mock target)
    - File contains `test_normal_output`, `test_all_fail_returns_safe_defaults`, `test_timeout_fallback`, `test_partial_failure`, `test_diff_lines_sum`, `test_clean_working_dir`, `test_first_commit_time_earliest`, `test_git_not_installed`, `test_non_git_dir`, `test_no_commits_today`
    - Running `python3 -m pytest tests/test_git_context.py -v` shows errors or failures (RED phase - module not yet created)
  </acceptance_criteria>
  <done>tests/test_git_context.py has 13+ test functions covering all STA-03 scenarios. Tests fail because core/git_context.py does not exist yet (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement core/git_context.py to pass all tests (GREEN)</name>
  <files>core/git_context.py</files>
  <read_first>
    - tests/test_git_context.py (the tests just written — implementation must satisfy these exact assertions)
    - core/trigger.py (reference for module style: single-line docstrings, type annotations, snake_case)
    - .planning/phases/02-git-context/02-RESEARCH.md (Architecture Patterns section — complete reference implementation)
    - .planning/phases/02-git-context/02-CONTEXT.md (D-01 through D-05 locked decisions)
    - .planning/codebase/CONVENTIONS.md (error handling pattern, naming, docstring format)
  </read_first>
  <behavior>
    - load_git_context(cwd) returns dict with keys: commits_today (int), diff_lines (int), first_commit_time (str|None), repo_path (str|None)
    - _run_git_cmd(args, cwd) returns stdout.strip() on success, None on any failure
    - _parse_commits_today(output) returns len(lines) or 0
    - _parse_diff_lines(output) returns sum of insertions + deletions from regex match on last line
    - _parse_first_commit_time(output) returns lines[-1].strip() (earliest commit) or None
    - _parse_repo_path(output) returns output.strip() or None
    - ThreadPoolExecutor(max_workers=4) runs all 4 subprocesses in parallel
    - All exceptions caught — function never raises
  </behavior>
  <action>
    Create `core/git_context.py` implementing `load_git_context(cwd: str) -> dict`:

    1. Module header (matching project convention):
       ```python
       # core/git_context.py
       ```

    2. Imports:
       ```python
       import re
       import subprocess
       from concurrent.futures import ThreadPoolExecutor, as_completed
       ```

    3. Module-level constants:
       ```python
       SUBPROCESS_TIMEOUT = 5
       _SAFE_DEFAULT = {
           'commits_today': 0,
           'diff_lines': 0,
           'first_commit_time': None,
           'repo_path': None,
       }
       ```

    4. `_run_git_cmd(args: list[str], cwd: str) -> str | None`:
       - Call `subprocess.run(args, cwd=cwd, capture_output=True, text=True, timeout=SUBPROCESS_TIMEOUT, check=False)` (per D-04, RESEARCH Pattern 1)
       - If `result.returncode == 0`: return `result.stdout.strip()`
       - Otherwise: return `None`
       - Wrap entire body in `try/except Exception: return None` to catch TimeoutExpired, FileNotFoundError, OSError, etc.
       - Single-line docstring: `"""Run a git command in cwd; return stdout on success, None on any failure."""`

    5. Parse functions (all module-private with underscore prefix):

       `_parse_commits_today(output: str | None) -> int`:
       - If `not output`: return 0
       - Return `len(output.splitlines())`
       - Docstring: `"""Count lines in git log --oneline output."""`

       `_parse_diff_lines(output: str | None) -> int`:
       - If `not output`: return 0 (handles clean working directory — Pitfall 1)
       - Take `last_line = output.splitlines()[-1]`
       - Use `re.search(r'(\d+) insertion', last_line)` to extract insertions (handles singular/plural)
       - Use `re.search(r'(\d+) deletion', last_line)` to extract deletions
       - Return `insertions + deletions` (per D-02)
       - Docstring: `"""Parse insertions + deletions from git diff --stat output."""`

       `_parse_first_commit_time(output: str | None) -> str | None`:
       - If `not output`: return None
       - `lines = output.splitlines()`
       - Return `lines[-1].strip() if lines else None` (per D-05 note and Pitfall 5: last line = earliest commit)
       - Docstring: `"""Extract earliest commit timestamp from git log --format=%ai output."""`

       `_parse_repo_path(output: str | None) -> str | None`:
       - Return `output.strip() if output else None`
       - Docstring: `"""Return repo root path from git rev-parse --show-toplevel output."""`

    6. Dispatch dict for parsing:
       ```python
       _PARSERS = {
           'commits_today': _parse_commits_today,
           'diff_lines': _parse_diff_lines,
           'first_commit_time': _parse_first_commit_time,
           'repo_path': _parse_repo_path,
       }
       ```

    7. `load_git_context(cwd: str) -> dict`:
       - Docstring: `"""Return git context dict for cwd. Never raises; returns safe defaults on any failure."""`
       - Define tasks dict mapping key -> (args_list, cwd):
         - `'commits_today'`: `['git', 'log', '--oneline', '--since=midnight']`
         - `'diff_lines'`: `['git', 'diff', '--stat', 'HEAD']`
         - `'first_commit_time'`: `['git', 'log', '--format=%ai', '--since=midnight']`
         - `'repo_path'`: `['git', 'rev-parse', '--show-toplevel']`
       - Initialize `results = dict(_SAFE_DEFAULT)`
       - Use `ThreadPoolExecutor(max_workers=4)` with `as_completed` (per RESEARCH Pattern 2)
       - For each completed future, wrap `future.result()` in try/except Exception (Pitfall 3)
       - Parse with `_PARSERS[key](output)` and assign to `results[key]`
       - Return `results`

    After writing, run: `python3 -m pytest tests/test_git_context.py -v` to confirm all tests pass (GREEN).
    Then run: `python3 -m pytest tests/ -v` to confirm no regressions in existing tests.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_git_context.py -v && python3 -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - core/git_context.py exists
    - File contains `SUBPROCESS_TIMEOUT = 5` (module-level constant, SCREAMING_SNAKE_CASE)
    - File contains `def load_git_context(cwd: str) -> dict:` (public API with type annotations)
    - File contains `def _run_git_cmd(args: list[str], cwd: str) -> str | None:` (private helper)
    - File contains `subprocess.run(` with `timeout=SUBPROCESS_TIMEOUT` and `check=False`
    - File contains `ThreadPoolExecutor(max_workers=4)`
    - File contains `as_completed` (not futures.map — per RESEARCH anti-pattern)
    - File contains `re.search(r'(\d+) insertion'` for diff parsing (per D-02)
    - File contains `re.search(r'(\d+) deletion'` for diff parsing (per D-02)
    - File contains `_SAFE_DEFAULT` dict with keys commits_today, diff_lines, first_commit_time, repo_path
    - File contains `except Exception:` in _run_git_cmd (broad catch for subprocess failures per Pitfall 2)
    - `python3 -m pytest tests/test_git_context.py -v` exits 0 with all tests passing
    - `python3 -m pytest tests/ -v` exits 0 with no regressions (all existing tests still pass)
    - File does NOT contain `import asyncio` (per RESEARCH: ThreadPoolExecutor chosen over asyncio)
    - File does NOT contain `check_output` (per RESEARCH anti-pattern)
    - Every public function has a single-line docstring (per CONVENTIONS.md)
  </acceptance_criteria>
  <done>core/git_context.py implements load_git_context with 4 parallel subprocesses. All tests in test_git_context.py pass (GREEN). Full test suite passes with no regressions. STA-03 requirement satisfied: any subprocess failure silently returns safe defaults.</done>
</task>

</tasks>

<verification>
1. `python3 -m pytest tests/test_git_context.py -v` — all new tests pass
2. `python3 -m pytest tests/ -v` — full suite green, no regressions
3. `python3 -c "from core.git_context import load_git_context; print(load_git_context('.'))"` — returns valid dict from actual repo (integration smoke test)
4. `python3 -c "from core.git_context import load_git_context; print(load_git_context('/tmp'))"` — returns safe defaults for non-git dir
</verification>

<success_criteria>
- core/git_context.py exports load_git_context(cwd: str) -> dict
- 4 parallel subprocesses: git log --oneline, git diff --stat, git log --format=%ai, git rev-parse --show-toplevel
- Any subprocess failure (timeout, FileNotFoundError, non-zero exit) silently returns field default (STA-03)
- diff_lines = insertions + deletions (D-02)
- first_commit_time = earliest commit today, not latest (D-05 + Pitfall 5)
- All new tests pass, full suite green
- No third-party dependencies (stdlib only)
</success_criteria>

<output>
After completion, create `.planning/phases/02-git-context/02-01-SUMMARY.md`
</output>
